services:
  - type: web
    name: chatbot-mistral
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MISTRAL_API_KEY
        value: k1T5pJrbwL5mdIPyeLgt44vBeS8qMdcF
